//lib
import { Theme } from "@material-ui/core";
import { makeStyles } from "@material-ui/core/styles";

export const useToursHighlightStyles = makeStyles<Theme>({
  tourHighlightsContainer: {
    minWidth: "100%",

  },
});
